# Python code standards

## Style

- PEP 8 compliant with 120-char line length (configured in pyproject.toml)
- Google-style docstrings for all public classes and functions
- Type hints where appropriate

## Formatting and linting

- **ruff:** Lint (`ruff check .`)
  - Rules enabled: E (pycodestyle errors), W (warnings), F (pyflakes), N (pep8-naming), B (bugbear), C4 (comprehensions)
  - Max McCabe complexity: 10
  - Excludes: .git, .venv, __pycache__, build, dist, hedweb/\_version.py
  - Per-file: F401 ignored in `__init__.py` (unused imports are re-exports)
- **ruff format:** Format (`ruff format .`); 120-char line, matches ruff lint config
- **typos:** Spell checking via `uvx typos`; domain-specific words (hed, parms, etc.) excluded via `[tool.typos.default.extend-words]` in pyproject.toml

## Naming conventions

- Classes: PascalCase (e.g., `HedString`, `HedSchema`, `TabularInput`)
- Functions/methods: snake_case
- Constants: UPPER_SNAKE_CASE
- Private members: leading underscore
- Test methods: may use camelCase (legacy convention; N802 ignored by ruff)

## Import order (ruff isort)

1. Standard library
2. Third-party packages
3. Local `hedweb` package (`known-first-party = ["hedweb"]`)

## Dependencies

- **Core:** Flask>=3.1.1, Flask-WTF>=1.2.2, hedtools>=0.9.0, openpyxl>=3.1.5, pandas>=2.2.3,\<3.0.0, Werkzeug>=3.1.3, requests>=2.32.4, table-remodeler>=0.2.0
- **Dev:** ruff (>=0.8.0), coverage>=7.0.0, typos>=1.29.0, mdformat==0.7.22, mdformat-myst==0.3.0
- **Docs:** sphinx (\<10), furo, sphinx-copybutton, myst-parser, sphinx-autodoc-typehints, linkify-it-py
- **Target:** Python 3.10+

## Common patterns

- Flask `app_factory.py` pattern; create_app() factory function
- Business logic in `*_operations.py` modules, not in route handlers
- Flask-WTF for CSRF-protected form handling
- JSON responses with consistent structure from REST endpoints
- `hedweb/_version.py` is auto-generated by setuptools-scm; do not edit

## Quality metrics (qlty.toml)

- Max argument count: 4
- Max method complexity: 5
- Max method lines: 50
- Max file lines: 300
- Max method count per class: 20
- Max nested control flow: 4
- Max return statements: 4
- Similar/identical code detection enabled
